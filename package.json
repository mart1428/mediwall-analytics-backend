{
  "name": "mediwall-analytics-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Anonymous analytics + feedback ingest for the MediWall app. Stores only aggregate, PHI-free counts.",
  "main": "src/server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "smoke": "node scripts/smoke.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0",
    "mongodb": "^6.8.0"
  }
}
